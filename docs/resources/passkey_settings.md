---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "descope_passkey_settings Resource - descope"
subcategory: ""
description: |-
  Manages the project-level passkey (WebAuthn) authentication settings. This is a singleton resource, and its id is always the project ID.
---

# descope_passkey_settings (Resource)

Manages the project-level passkey (WebAuthn) authentication settings. This is a singleton resource, and its id is always the project ID.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (String) The ID of the project that these settings belong to. Changing this value will require the resource to be deleted and recreated.

### Optional

- `android_fingerprints` (Set of String) A list of SHA-256 APK key hash fingerprints (colon-separated hex, e.g. `AB:CD:EF:...`) that are allowed as passkey origins for Android apps. When set, only Android apps with a matching fingerprint will be permitted to use passkey authentication.
- `disabled` (Boolean) Setting this to `true` will disallow using passkey authentication directly via API and SDK calls. Note that this does not affect authentication flows that are configured to use passkeys.
- `display_name` (String) The human-friendly name shown to users when they create or use a passkey. Some password managers display this name, while others display the top level domain instead. When left empty, the project name is used.
- `top_level_domain` (String) Passkeys will be usable in the following domain and all its subdomains.

### Read-Only

- `id` (String) The ID of this resource.
