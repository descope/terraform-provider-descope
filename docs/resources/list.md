---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "descope_list Resource - descope"
subcategory: ""
description: |-
  Manages a single list of allowed or denied values in a Descope project. A list holds either plain text values, IP addresses and CIDR ranges, or an arbitrary JSON object, and can be referenced from flows.
---

# descope_list (Resource)

Manages a single list of allowed or denied values in a Descope project. A list holds either plain text values, IP addresses and CIDR ranges, or an arbitrary JSON object, and can be referenced from flows.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the list. Maximum length is 100 characters.
- `project_id` (String) The ID of the project that this list belongs to. Changing this value will require the resource to be deleted and recreated.

### Optional

- `description` (String) An optional description for the list. Defaults to an empty string if not provided.
- `ips` (Set of String) A set of IP addresses and CIDR ranges. Each value must be a valid IP address or CIDR range. Exactly one of `texts`, `ips` or `json` must be set.
- `json` (String) An arbitrary JSON object, given as a JSON string. Exactly one of `texts`, `ips` or `json` must be set. The document is compared by content, so reindenting it or reordering its keys is not a change.
- `texts` (Set of String) A set of text values. Exactly one of `texts`, `ips` or `json` must be set, and which one is set determines the kind of list. Duplicate values are collapsed by the set, matching the backend which stores each value once.

### Read-Only

- `id` (String) The ID of this resource.
